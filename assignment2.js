//Array Manipulation
//program to merge two arrays into a single array
//function mergeArrays(arr1, arr2) {
  //  return arr1.concat(arr2); 
  //}
  //var array1 = [1, 2, 3];
  //var array2 = [4, 5, 6];
  //var mergedArray = mergeArrays(array1, array2);
  //console.log("Merged array:", mergedArray); 

  // program to find the largest number in an array
  //function findLargestNumber(numbers) {
    //var largest = numbers[0]; 
    //for (var i = 1; i < numbers.length; i++) {
      //if (numbers[i] > largest) {
        //largest = numbers[i]; 
      //}
    //}
    //return largest; 
  //}
  //var inputArray = [4, 2, 6, 8];
  //var largestNumber = findLargestNumber(inputArray);
 // console.log("The largest number is:", largestNumber);

 //program to sort an array of strings alphabetically
 //function sortStringsAlphabetically(strings) {
   // return strings.sort(); 
  //}
  //var inputArray = ['d', 'e', 'f', 'a'];
  //var sortedArray = sortStringsAlphabetically(inputArray);
  //console.log("Sorted array:", sortedArray);
  
  //program to remove duplicates from an array of numbers
  //function removeDuplicates(numbers) {
    //var uniqueNumbers = {};
    //for (var i = 0; i < numbers.length; i++) {
     // uniqueNumbers[numbers[i]] = true;
    //}
    //var uniqueArray = Object.keys(uniqueNumbers);
    //return uniqueArray;
  //}
  //var inputArray = ['a', 'a', 'b', 'd', 'e', 'e', 'f'];
  //var uniqueNumbers = removeDuplicates(inputArray);
  //console.log("Array with duplicates removed:", uniqueNumbers);
  
//  program to filter out even numbers from an array of numbers
//function getEvenNumbers(numbers) {
   // var evenNumbers = []; 
    //for (var i = 0; i < numbers.length; i++) {
      //if (numbers[i] % 2 === 0) { 
        //evenNumbers.push(numbers[i]); 
      //}
   // }
   // return evenNumbers; 
 // }
 // var inputArray = [1, 2, 3, 4, 5, 6, 7, 8, 9];
 // var evenNumbersArray = getEvenNumbers(inputArray);
 // console.log("Array with only even numbers:", evenNumbersArray); 
